import styles from './AppName.module.css';

function AppName() {
    return (
        <div className="row">
            <div className={`${styles.todoHeading} mb-4`}>ToDo React App</div>
        </div>
    )
}

export default AppName;